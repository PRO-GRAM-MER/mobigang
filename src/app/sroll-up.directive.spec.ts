import { SrollUpDirective } from './directives/sroll-up.directive';

describe('SrollUpDirective', () => {
  it('should create an instance', () => {
    const directive = new SrollUpDirective();
    expect(directive).toBeTruthy();
  });
});
